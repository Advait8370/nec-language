# NEC — Next-gen Code

NEC is an independent programming language designed for AI, ML, and data workflows.

## Features
- Independent interpreter (not Python exec)
- IDLE-style GUI
- Separate NEC Console (REPL)
- Package manager (`necpkg`)
- Line-numbered errors
- Windows `.exe` support

## Run NEC
```bash
python nec.py run examples/demo.nec
